```markdown
# 透明幕布手势交互数字艺术作品 PRD
## 1. 产品概述
### 1.1 产品名称
VibeCoding 透明幕布手势交互装置
### 1.2 产品定位
基于浏览器端实时手部追踪的创意交互数字艺术作品，用户通过"捏合"手势擦除虚拟幕布，显露背后自定义上传的视频内容，实现"将代码把海藏在现实中"的沉浸式体验。
### 1.3 目标用户
- 数字艺术爱好者
- 创意开发者
- 线下展览参观者
- 短视频内容创作者

## 2. 需求目标
### 2.1 功能目标
1. 实时摄像头画面采集与显示
2. 单手握持/捏合手势识别与追踪
3. 幕布层手势擦除效果（带自然边缘羽化）
4. 自定义视频上传与循环播放
5. 擦除区域自动恢复功能（可选）
6. 基础交互提示动画
### 2.2 体验目标
1. 手势识别响应延迟 < 100ms
2. 擦除动画帧率稳定在 50-60fps
3. 支持 720p 及以上分辨率视频播放
4. 兼容主流桌面端 Chrome/Edge 浏览器

## 3. 视觉与交互规范
### 3.1 分层设计规范
| 层级 | 名称 | 初始状态 | 技术实现 | 关键属性 |
|------|------|----------|----------|----------|
| 1 | 背景层 | 实时摄像头画面 | Canvas 2D | 全屏显示，z-index: 1 |
| 2 | 视频层 | 完全不可见 | Canvas 2D | 全屏拉伸，z-index: 2 |
| 3 | 幕布层 | 全屏幕覆盖 | Canvas 2D | 初始颜色：rgba(255,255,255,0.9)，z-index: 3 |
| 4 | 交互提示层 | 微弱闪烁 | CSS + Canvas | 手部轮廓动画，z-index: 4 |

### 3.2 交互流程规范
1. **初始状态**：显示摄像头实时画面 + 半透明白色幕布 + 手势引导提示
2. **手势检测**：当检测到单只手且拇指与食指距离 < 30px 时，进入擦除模式
3. **擦除过程**：以食指指尖为中心绘制羽化笔触，使用 `destination-out` 混合模式擦除幕布
4. **停止交互**：手势消失或松开捏合时，停止擦除，可选启动自动恢复
5. **自动恢复**：擦除区域以 0.01/帧 的透明度缓慢恢复为初始幕布状态

### 3.3 动效参数规范
#### 3.3.1 擦除笔触参数
- 基础大小：20px
- 速度影响系数：0.8
- 边缘羽化系数：0.6（笔触大小 × 羽化系数）
- 透明度渐变：中心 100% → 边缘 0%（径向渐变）

#### 3.3.2 动画缓动参数
- 手势跟随：`power2.out`（GSAP 缓动）
- 自动恢复：线性缓动，时长 4 秒
- 状态切换：`power1.inOut`，时长 0.2 秒

#### 3.3.3 物理参数
- 惯性系数：0.85
- 摩擦力：0.95
- 手势识别置信度阈值：0.7
- 捏合距离阈值：30px

## 4. 技术实现方案
### 4.1 最终技术栈
核心：HTML5 Canvas 2D + MediaPipe Hands 0.4.16
动效：GSAP 3.12.2
媒体：WebRTC getUserMedia API
构建：原生 JavaScript（无框架依赖）

### 4.2 技术选型对比结论
| 方案 | 排除原因 | 入选理由 |
|------|----------|----------|
| 纯 CSS | 无法实现动态像素级擦除 | - |
| SVG | 复杂混合模式支持差，性能低 | - |
| WebGL | 学习曲线陡峭，开发成本高 | - |
| Canvas 2D | - | 像素级控制精确，混合模式完善，性能满足需求 |
| MediaPipe Hands | - | 浏览器端实时手部追踪，准确率高，轻量易用 |
| GSAP | - | 时间线管理优秀，缓动函数丰富，性能优于原生动画 |

### 4.3 核心技术原理
1. **多层 Canvas 叠加**：背景层、视频层、幕布层独立渲染，互不干扰
2. **混合模式擦除**：`globalCompositeOperation = 'destination-out'` 实现像素透明
3. **坐标映射**：将 MediaPipe 输出的归一化坐标（0-1）转换为屏幕像素坐标
4. **渲染循环**：`requestAnimationFrame` 驱动 60fps 实时渲染
5. **动态笔触**：根据手部移动速度调整笔触大小，模拟真实画笔质感

## 5. 核心功能模块设计
### 5.1 初始化模块
#### 5.1.1 摄像头初始化
- 输入：无
- 输出：MediaStream 对象
- 功能：请求用户摄像头权限，初始化视频流
- 错误处理：摄像头权限拒绝时显示提示信息

#### 5.1.2 手部追踪初始化
- 输入：MediaPipe 配置参数
- 输出：Hands 实例
- 配置：`{ maxNumHands: 1, modelComplexity: 1, minDetectionConfidence: 0.7, minTrackingConfidence: 0.5 }`

#### 5.1.3 Canvas 初始化
- 输入：窗口宽高
- 输出：三个 Canvas 上下文对象
- 功能：设置 Canvas 尺寸，绘制初始幕布

### 5.2 手势识别模块
#### 5.2.1 手部关键点解析
- 输入：MediaPipe 检测结果
- 输出：食指指尖(8)、拇指指尖(4)坐标
- 功能：提取手部 21 个关键点中的目标点

#### 5.2.2 捏合手势判断
- 输入：食指与拇指坐标
- 输出：布尔值（是否捏合）
- 算法：`distance = Math.sqrt(Math.pow(x1-x2, 2) + Math.pow(y1-y2, 2))`

### 5.3 擦除渲染模块
#### 5.3.1 动态笔触计算
- 输入：当前坐标、上一帧坐标
- 输出：笔触大小
- 算法：`velocity = Math.sqrt(Math.pow(x-lastX, 2) + Math.pow(y-lastY, 2))`；`brushSize = baseSize + velocity * scaleFactor`

#### 5.3.2 羽化笔触绘制
- 输入：坐标、笔触大小
- 功能：绘制带径向渐变和阴影模糊的圆形笔触
- 关键代码：`ctx.shadowBlur = brushSize * 0.6`；`ctx.shadowColor = 'rgba(0,0,0,1)'`；`ctx.beginPath()`；`ctx.arc(x, y, brushSize, 0, Math.PI * 2)`；`ctx.fill()`

### 5.4 视频管理模块
#### 5.4.1 视频上传处理
- 输入：File 对象
- 输出：HTMLVideoElement
- 功能：创建视频元素，设置循环、静音、自动播放

#### 5.4.2 视频帧渲染
- 输入：视频元素
- 功能：每帧将视频画面绘制到视频层 Canvas

### 5.5 自动恢复模块
- 输入：是否擦除状态
- 功能：停止擦除时，每帧绘制低透明度幕布
- 参数：恢复速度 0.01/帧（rgba 透明度）

## 6. 验收标准
### 6.1 功能验收
1. 打开页面后自动请求摄像头权限，授权后显示实时画面
2. 做出捏合手势时，指尖位置出现擦除效果
3. 移动手部时，擦除轨迹连续自然，无明显卡顿
4. 上传本地视频后，擦除区域显示视频内容，视频循环播放
5. 停止手势后，擦除区域在 4 秒内完全恢复
6. 手势消失后，自动停止擦除并启动恢复

### 6.2 性能验收
1. Chrome 110+ 浏览器中，平均帧率 ≥ 55fps
2. 手势识别响应延迟 ≤ 100ms
3. 720p 视频播放时，CPU 使用率 ≤ 60%（中端笔记本）
4. 连续擦除 1 分钟，无内存泄漏

### 6.3 视觉验收
1. 擦除边缘羽化自然，无明显锯齿
2. 视频与背景层对齐准确，无拉伸变形
3. 幕布透明度适中，既能遮挡视频又不影响背景可见度
4. 交互提示动画清晰，不干扰主视觉

## 7. 开发排期（总周期 4 周）
| 阶段 | 时间 | 交付物 |
|------|------|--------|
| 基础搭建 | 第1周 | 摄像头采集、Canvas 分层、MediaPipe 集成 |
| 核心功能 | 第2周 | 手势识别、擦除效果、视频上传播放 |
| 优化测试 | 第3周 | 性能优化、动效调优、兼容性测试 |
| 上线准备 | 第4周 | 代码打包、文档编写、部署上线 |

## 8. 风险评估与应对
| 风险 | 影响程度 | 应对措施 |
|------|----------|----------|
| 低性能设备卡顿 | 中 | 1. 提供分辨率切换选项 2. 限制最大笔触数量 3. 降低模型复杂度至 0 |
| 浏览器兼容性 | 中 | 1. 明确支持 Chrome/Edge 110+ 2. 提供降级方案（静态展示） |
| 手势识别准确率 | 低 | 1. 调整置信度阈值 2. 添加手势引导提示 3. 支持自定义捏合距离 |
| 视频格式不支持 | 低 | 1. 限制上传格式为 MP4/WebM 2. 提供格式转换提示 |

## 9. 扩展功能规划
1. 双手同时擦除功能
2. 多种擦除形状（方形、星形、自定义图案）
3. 擦除粒子效果（水花、星光）
4. 3D 幕布变形效果
5. 擦除画面保存与分享
6. 手势切换功能（张开手掌清除全部）
```